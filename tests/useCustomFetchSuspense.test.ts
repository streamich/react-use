import { useCustomFetchSuspense } from '../src/useCustomFetchSuspense'

describe('useCustomFetchSuspense', () => {
  it('hook is defined', () => {
    expect(useCustomFetchSuspense).toBeDefined();
  });
});
